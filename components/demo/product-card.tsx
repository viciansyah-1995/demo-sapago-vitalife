'use client';
import Image from 'next/image';
import {ArrowUpRight,ArrowRight} from 'lucide-react';
import {type Product,price} from '@/lib/demo/data';
export function ProductCard({product,onOpen,compact=false}:{product:Product;onOpen:(p:Product)=>void;compact?:boolean}){return compact?<button className="recommendation-card" onClick={()=>onOpen(product)}><div className="recommendation-image"><Image src="/images/wellness-hero.png" alt={`Kemasan ilustrasi Vitalife ${product.name}`} fill sizes="80px" style={{objectFit:'cover',objectPosition:product.position}}/></div><div><small>PILIHAN DARI KATALOG DEMO</small><strong>Vitalife {product.name}</strong><span>{price(product.price)} <em>· Harga demo</em></span><b>Lihat Produk <ArrowUpRight size={12}/></b></div></button>:<article className="product-card"><button className="product-image" style={{background:product.color}} onClick={()=>onOpen(product)} aria-label={`Lihat detail ${product.name}`}><Image src="/images/wellness-hero.png" alt={`Ilustrasi kemasan Vitalife ${product.name}, bukan produk resmi`} fill sizes="(max-width:760px) 45vw, 24vw" style={{objectFit:'cover',objectPosition:product.position}}/><span className="product-demo-tag">DEMO PRODUCT</span><span className="product-arrow"><ArrowUpRight size={17}/></span></button><span className="product-category">{product.category}</span><h3>Vitalife {product.name}</h3><p>{product.description}</p><div className="product-bottom"><span>{price(product.price)}<small>Harga demo</small></span><button onClick={()=>onOpen(product)}>Lihat Detail <ArrowRight size={13}/></button></div></article>}
